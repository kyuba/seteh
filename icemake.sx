(library "seteh" libcurie
  (name "Seteh")
  (description "Shell Utilities in Library Form")
  (version "1")
  (url "http://kyuba.org/")

  (code "tar" "patch" "lambda" "lambda-environment" "lambda-lambda-mu"
        "lambda-promise" "lambda-eval" "lambda-state")

  (headers
        "tar" "patch" "lambda")

  (test-cases
        "lambda")

;  (documentation
;        "description")
)
