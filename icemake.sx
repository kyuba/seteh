;;
; vim:ft=lisp
;;

(library "seteh" libcurie
  (name "Seteh")
  (description "Shell Utilities in Library Form")
  (version "1")
  (url "http://kyuba.org/")

  (code "tar" "patch" "lambda" "lambda-environment" "lambda-lambda-mu"
        "lambda-eval" "lambda-state")

  (headers
        "tar" "patch" "lambda")

;  (documentation
;        "description")
)

(test-case "lambda" libcurie (libraries "seteh") (code "test-lambda"))
